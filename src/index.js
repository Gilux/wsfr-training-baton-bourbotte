const { matches, clubs } = require("./fixtures.js");
const { BatonBourbotte } = require("./baton.js");

const seasonWithLyonAsFirstHolder = new BatonBourbotte(clubs, matches, "OL");

// You can play with the code here instead of running tests.
// simply run `node src/index.js`
